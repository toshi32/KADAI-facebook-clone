<h1>ブログ詳細画面</h1>
<p>タイトル: <%= @blog.title %></p>
<p>内容: <%= @blog.content %></p>

<h1>投稿一覧</h1>
<p><%= notice %></p>
<tr>
  <th>投稿</th>
</tr>
<% @blogs.each do |blog| %>
<tr>
  <td><%= blog.content %></td>
  <td><%= link_to '投稿内容の詳細確認', blog_path(blog.id) %></td>
  <td><%= link_to "投稿を編集する", edit_blog_path(blog.id) %></td>
  <td><%= link_to '投稿を削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
</tr>
<% end %>
</table>
<br>
<%= link_to '新しく投稿をする', new_blog_path %>
<br><br><br>